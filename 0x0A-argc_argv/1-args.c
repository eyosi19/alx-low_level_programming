#include "main.h"
#include <stdio.h>

/**
 * main - returns the number of the arguments
 *
 * @argc: number of arguments
 *
 * @argv: array of arguments
 *
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%d\n", argc - 1);
	return (0);
}
